{
  "name": "image-lambda",
  "version": "1.0.0",
  "description": "Lab 17 - S3 bucket that allows user to upload image and when they do, it will trigger a Lambda function",
  "main": "index.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "node-fetch": "^3.3.1"
  }
}
